





<% 
if (typeof datas == 'object' && datas && datas.length > 0) { 
	data_parent_id = datas[0].options_product_speciality_parent_id;
	data_id = datas[0].options_product_speciality_ID ;
}else{ 
	let DanhMucChungMacDinhID = 0;
	data_parent_id = DanhMucChungMacDinhID;
	data_id = DanhMucChungMacDinhID;
}
if (typeof option_list == 'object' && 
option_list  && option_list.length > 0) { } else {option_list = [];}
%>	





<option value="0">Chọn option cha</option>
<% 
if( option_list.length > 0){
for(var  x in option_list) { %>
	<% 
	if(option_list[x].options_product_speciality_parent_id == 0) { 
	%>
		<% 
		if(option_list[x].options_product_speciality_ID  == data_parent_id ){ 	%>
			<option  style="font-weight:bold;color:red;"  
			selected value="<%= option_list[x].options_product_speciality_ID %>">
			<%= option_list[x].options_product_speciality_name %></option>
		<% 
		}else if(option_list[x].options_product_speciality_ID  == data_id){ %>
			<option value ="<%= option_list[x].options_product_speciality_ID %>" 
			disabled><%= option_list[x].options_product_speciality_name %></option>
		<% 
		}else{ 	%>
			<option value ="<%= option_list[x].options_product_speciality_ID %>">
			<%= option_list[x].options_product_speciality_name %></option>
		<% 
		} 
		%>			
    <% 
    } 
	%>
<% 
} 
%>
<% 
} 
%>